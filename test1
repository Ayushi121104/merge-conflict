#include <stdio.h>
int main() {    

    int a1, b1, sum;
    
    printf("Enter two integers: ");
    scanf("%d %d", &a1, &b1);

    // calculate the sum
    sum = a1 + b1;      
    
    printf("%d + %d = %d", a1, b1, sum);
    return 0;
}

